# 华美机场解析系统 - bin目录

## 📁 目录结构

```
bin/
├── config/                    # 配置文件
│   ├── config.js             # 系统配置
│   └── cryptoUtils.js        # 加密工具
├── data/                     # 数据目录
│   ├── cities.csv            # 机场城市数据
│   ├── cities.json           # 机场城市数据(JSON格式)
│   ├── generated/            # 代码生成的数据
│   │   ├── lounges/          # 贵宾厅数据
│   │   ├── codes/            # 贵宾厅代码
│   │   └── intermediate/     # 中间数据
│   ├── exports/              # 导出文件
│   │   ├── csv/              # CSV格式导出
│   │   ├── excel/            # Excel格式导出
│   │   └── json/             # JSON格式导出
│   └── temp/                 # 临时文件
│       ├── logs/             # 日志文件
│       └── *.json            # 临时JSON文件
└── scripts/                  # 脚本文件
    ├── getDomesticLounges.js # 获取所有机场贵宾厅数据
    ├── getAllLounges.js      # 获取所有机场贵宾厅数据(旧版)
    ├── getCityData.js        # 获取城市数据
    ├── getLoungeData.js      # 获取贵宾厅数据
    ├── getLoungeDetail.js    # 获取贵宾厅详情
    ├── getUserInfo.js        # 获取用户信息
    ├── createOrder.js        # 创建订单
    ├── package.json          # 依赖配置
    └── node_modules/         # 依赖包
```

## 🚀 使用方法

### 1. 获取所有机场贵宾厅数据
```bash
cd scripts
node getDomesticLounges.js
```

### 2. 查看帮助信息
```bash
cd scripts
node getDomesticLounges.js --help
```

## 📊 输出文件说明

### 生成的数据文件
- `data/generated/lounges/all-lounges.json` - 完整贵宾厅数据(JSON格式)
- `data/generated/codes/all-lounge-codes.json` - 贵宾厅代码列表(JSON格式)

### 导出的文件
- `data/exports/csv/all-lounges.csv` - 完整贵宾厅数据(CSV格式)
- `data/exports/excel/all-lounges.xlsx` - 完整贵宾厅数据(Excel格式)
- `data/exports/excel/all-lounge-codes.xlsx` - 贵宾厅代码列表(Excel格式)

### 中间文件
- `data/generated/lounges/all-lounges-intermediate.json` - 中间保存的贵宾厅数据
- `data/generated/codes/all-lounge-codes-intermediate.json` - 中间保存的贵宾厅代码

## 🔧 配置说明

### 参数设置
- `domesticForeign`: "1" = 国内机场, "2" = 国外机场
- `continentType`: 根据国家代码自动设置大洲类型

### 支持的大洲类型
- AS - 亚洲
- EU - 欧洲  
- NA - 北美洲
- SA - 南美洲
- AF - 非洲
- OC - 大洋洲

## 📝 注意事项

1. 脚本会自动识别国内和国外机场并设置相应参数
2. 每10个机场自动保存中间数据防止数据丢失
3. 会自动跳过重复的机场
4. 脚本会自动添加请求延迟避免过于频繁
5. 所有生成的文件都会保存在对应的目录中，保持结构清晰

## 🗂️ 文件清理

定期清理以下目录中的文件：
- `data/temp/` - 临时文件
- `data/generated/intermediate/` - 中间数据文件
